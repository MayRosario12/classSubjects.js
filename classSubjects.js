// Collect user information using prompt()
let subjectTitle = prompt("DSA:");
let classSchedule = prompt("8-9:00 AM - 11:12:30 AM, MWF:");
let classroom = prompt("CLB:");
let classInstructor = prompt("CO PASCUA:");
let studentName = prompt("May rosario:");

//Log the information to the console
console.log(`${studentName} is currently enrolled in $
{subjectTitle} with a class schedule of ${classSchedule} at room $
{classroom}. The instructor for the subject is ${classInstructor}`);

